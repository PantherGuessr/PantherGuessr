"use client";

import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet';

const InteractableMap = () => {
    return (
        <div className="flex min-h-full min-w-full grow bg-black">
            element
        </div>
    );
}

export default InteractableMap;